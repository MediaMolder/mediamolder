upstream mediamolder {
    server api1:8080;
    server api2:8080;
}

server {
    listen 80;

    location / {
        proxy_pass         http://mediamolder;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 60s;
    }
}
