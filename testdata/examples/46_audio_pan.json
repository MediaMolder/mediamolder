{
  "schema_version": "1.0",
  "description": "Wave 7 #41: pan filter — remix stereo to stereo with explicit channel matrix. Note: if you edit this JSON manually, you must also update or remove the ffmpeg_cmd field.",
  "ffmpeg_cmd": "ffmpeg -i {{input}} -filter_complex [0:a]pan=_pos0=stereo|c0=0.5*c0+0.5*c1|c1=0.5*c0+0.5*c1[pan0];[pan0]asetnsamples=n=1024:p=0[chunk0] -map 0:a:0 -map 0:v:0 -c:v copy -c:a aac -map_metadata 0 -map_chapters 0 {{output}}",
  "inputs": [
    {
      "id": "in0",
      "url": "{{input}}",
      "map_metadata": true,
      "map_chapters": true,
      "streams": [
        {
          "input_index": 0,
          "type": "video",
          "track": 0
        },
        {
          "input_index": 0,
          "type": "audio",
          "track": 0
        }
      ]
    }
  ],
  "graph": {
    "nodes": [
      {
        "id": "pan0",
        "type": "filter",
        "filter": "pan",
        "params": {
          "_pos0": "stereo|c0=0.5*c0+0.5*c1|c1=0.5*c0+0.5*c1"
        }
      },
      {
        "id": "chunk0",
        "type": "filter",
        "filter": "asetnsamples",
        "params": {
          "n": "1024",
          "p": "0"
        }
      }
    ],
    "edges": [
      {
        "from": "in0:v:0",
        "to": "out0:v",
        "type": "video"
      },
      {
        "from": "in0:a:0",
        "to": "pan0",
        "type": "audio"
      },
      {
        "from": "pan0",
        "to": "chunk0",
        "type": "audio"
      },
      {
        "from": "chunk0",
        "to": "out0:a",
        "type": "audio"
      }
    ]
  },
  "outputs": [
    {
      "id": "out0",
      "url": "{{output}}",
      "codec_video": "copy",
      "codec_audio": "aac"
    }
  ]
}
