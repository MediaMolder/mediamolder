{
  "schema_version": "1.0",
  "description": "Decode in.mp4 -> scale video to 640x480, convert framerate to 30fps (dropping or duplicating frames as needed), encode to H.264 with libx264, encode audio to AAC -> write to out.mp4. Note: if you edit this JSON manually, you must also update or remove the ffmpeg_cmd field.",
  "ffmpeg_cmd": "ffmpeg -i {{input}} -filter_complex [0:v]scale=h=480:w=640[scale0];[scale0]fps=fps=30[fps0] -map 0:a:0 -map 0:v:0 -c:v libx264 -c:a aac -map_metadata 0 -map_chapters 0 {{output}}",
  "inputs": [
    {
      "id": "in0",
      "url": "{{input}}",
      "map_metadata": true,
      "map_chapters": true,
      "streams": [
        {
          "input_index": 0,
          "type": "video",
          "track": 0
        },
        {
          "input_index": 0,
          "type": "audio",
          "track": 0
        }
      ]
    }
  ],
  "graph": {
    "nodes": [
      {
        "id": "scale0",
        "type": "filter",
        "filter": "scale",
        "params": {
          "w": "640",
          "h": "480"
        }
      },
      {
        "id": "fps0",
        "type": "filter",
        "filter": "fps",
        "params": {
          "fps": "30"
        }
      }
    ],
    "edges": [
      {
        "from": "in0:v:0",
        "to": "scale0",
        "type": "video"
      },
      {
        "from": "scale0",
        "to": "fps0",
        "type": "video"
      },
      {
        "from": "fps0",
        "to": "out0:v",
        "type": "video"
      },
      {
        "from": "in0:a:0",
        "to": "out0:a",
        "type": "audio"
      }
    ]
  },
  "outputs": [
    {
      "id": "out0",
      "url": "{{output}}",
      "codec_video": "libx264",
      "codec_audio": "aac"
    }
  ]
}
