{
  "schema_version": "1.1",
  "description": "Video face detection. Decode in.mp4 and run face_detect on every 10th frame (embeddings off for speed), writing each detected face to faces.jsonl. Analysis-only — an input plus one processor, no encoder or muxer (set output_file to an absolute path when running by hand). Requires a with_onnx build and bundled models (MEDIAMOLDER_FACE_MODELS).",
  "inputs": [
    {
      "id": "in0",
      "url": "{{input}}",
      "streams": [
        { "input_index": 0, "type": "video", "track": 0 }
      ]
    }
  ],
  "graph": {
    "nodes": [
      {
        "id": "detect_faces",
        "type": "go_processor",
        "processor": "face_detect",
        "params": {
          "every": 10,
          "conf": 0.5,
          "embeddings": false,
          "output_file": "faces.jsonl"
        }
      }
    ],
    "edges": [
      { "from": "in0:v:0", "to": "detect_faces:default", "type": "video" }
    ]
  },
  "outputs": []
}
