{
  "schema_version": "1.1",
  "description": "Encode a raw 320x240 yuv420p stream (testdata/sample.yuv) to MP4. Exercises Input.Kind = \"raw\" with the typed format / pixel_format / video_size / framerate fields (Wave 5 #23-#28). Matches 'ffmpeg -f rawvideo -pix_fmt yuv420p -s 320x240 -framerate 30 -i sample.yuv out.mp4'.",
  "inputs": [
    {
      "id": "in0",
      "url": "{{raw_yuv}}",
      "kind": "raw",
      "format": "rawvideo",
      "pixel_format": "yuv420p",
      "video_size": "320x240",
      "framerate": 30,
      "streams": [
        { "input_index": 0, "type": "video", "track": 0 }
      ]
    }
  ],
  "graph": {
    "nodes": [],
    "edges": [
      { "from": "in0:v:0", "to": "out0:v", "type": "video" }
    ]
  },
  "outputs": [
    {
      "id": "out0",
      "url": "{{output}}",
      "format": "mp4",
      "codec_video": "libx264",
      "encoder_params_video": { "preset": "ultrafast", "crf": "28" }
    }
  ]
}
