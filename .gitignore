# Binaries
mediamolder
*.exe
*.dll
*.so
*.dylib

# Build output
/bin/
/dist/

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html

# Go workspace
go.work
go.work.sum

# IDE
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test media corpus (handled by Git LFS)
# Keeping *.mp4, *.mkv etc. in LFS via .gitattributes

# Benchmark results (tracked separately)
bench-*.txt

# Internal planning docs (not for public release)
/private_local/

# Frontend build artefacts -- regenerated by `make build-gui`; embedded via
# //go:embed so there is no need to track them in git.
/internal/gui/dist/

# TypeScript incremental build cache
frontend/tsconfig.tsbuildinfo

# Large test media (not committed; fetched separately)
testdata/dental_video.mp4
testdata/dental_video_10s.mp4
testdata/dental_video_30s.mp4

# Stray pipeline / example processor output files when run from the repo root
/frame_info.jsonl
/scene_changes.jsonl
/detections.jsonl

docs/images/1.png
docs/images/2.png
docs/images/ABR.png
